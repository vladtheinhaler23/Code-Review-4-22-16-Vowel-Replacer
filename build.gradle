apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "codeReview42216"
version = '1.0'
mainClassName = "VowelReplace"

repositories {
  mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.+'
}
